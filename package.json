{
  "name": "dmrc",
  "version": "1.0.0",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend-employee && npm install && cd ../frontend-hr && npm install",
    "start": "npx concurrently \"cd backend && npx tsx src/index.ts\" \"cd frontend-employee && npm run dev -- --port 5173\" \"cd frontend-hr && npm run dev -- --port 5174\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
